[project]
name = "app"
version = "0.1"
requires-python = ">=3.10"
dependencies = [
  "pandas",
  "numpy",
  "jinja2",
  "IPython",
  "ipykernel",
]

[dependency-groups]
dev = [
  "pdoc",
  # lint
  "pylint==4.0.4",
  "rulebook-pylint==0.2",
  "regex",
]
