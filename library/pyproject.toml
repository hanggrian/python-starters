[tool.uv.workspace]
members = [
  "library",
  "library-extension",
]

[dependency-groups]
dev = [
  # main
  "pdoc",
  # lint
  "pylint==4.0.4",
  "rulebook-pylint==0.2",
  "regex",
  "stylebook==0.1",
  # test
  "pytest",
  "pytest-cov",
]
