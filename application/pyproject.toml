[project]
name = "application"
version = "0.1"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
  # lint
  "pylint==4.0.4",
  "rulebook-pylint==0.2",
  "regex",
  # test
  "pytest",
  "pytest-cov",
]
